import { DebouncedDirective } from './debounced.directive';

describe('DebouncedDirective', () => {
  it('should create an instance', () => {
    const directive = new DebouncedDirective();
    expect(directive).toBeTruthy();
  });
});
