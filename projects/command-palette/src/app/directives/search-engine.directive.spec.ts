import { SearchEngineDirective } from './search-engine.directive';

describe('SearchEngineDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchEngineDirective();
    expect(directive).toBeTruthy();
  });
});
